package com.factor.launcher.models

import android.graphics.drawable.Drawable
import android.view.View

class AppShortcut (val label : CharSequence, val icon : Drawable, val launchEvent : View.OnClickListener)